---
description: Sync JJ changes to Git with pre-sync validation
---

# /sync Command

Synchronize JJ (Jujutsu) changes to Git.

## Usage
- `/sync` - Sync current changes
- `/sync --squash` - Squash before sync
- `/sync --dry-run` - Preview only
- `/sync status` - Check sync status

## Request
$ARGUMENTS
